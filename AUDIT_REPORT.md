# 字幕翻译 Pipeline 代码审计报告 (Code Audit Report)

本报告针对当前字幕翻译流水线的架构实现、核心优势及“全局思维”缺失点进行深度分析，并提出改进建议。

---

## 1. 当前架构分析 (Current Architecture)

目前的翻译流程采用 **“分治 + 两阶段递进”** 模型：

1.  **全局术语提取 (Global Terms Extraction)**：采样全文，利用 LLM 进行 NER（命名实体识别）和术语提取。
2.  **梯次执行引擎 (Ladder Rescue Engine)**：支持动态调整批次大小（8->6->4->2->1），确保长文本处理的稳定性。
3.  **两步走翻译策略**：
    *   **直译阶段 (Literal Stage)**：保证 ID 对应和基本语义还原。
    *   **润色阶段 (Polish Stage)**：通过滑动窗口引入 `previous_context` 和 `future_context`，提升表达的地道性。

---

## 2. 核心审计发现

### ✅ 核心优势 (Strengths)
*   **高可用负载均衡**：`SmartLoadBalancer` 实现了多 API Key 调度、自动重试和健康检查，极大地降低了因 API 限制导致的失败率。
*   **上下文感知逻辑**：流水线通过异步预取 (Prefetch) 下一轮直译结果，在润色时能参考未来的语境，减少了上下文断层。
*   **格式严格校验**：通过 Tool Calling (Function Calling) 强制 LLM 输出 JSON，并配合 ID 匹配算法，有效解决了字幕翻译中常见的“行数不匹配”顽疾。

### ❌ 存在的短板 (Identified Gaps)
当前的 Pipeline 虽然在“局部语境”上处理得很好，但在 **“全局思维”** 上存在明显缺失：

1.  **角色特征与性别盲区 (Character & Gender Blindness)**
    *   **现象**：由于翻译是分块进行的，系统无法识别角色的性别。
    *   **后果**：中文翻译中“他/她/它”常出现混用，且角色语气（如：长辈对晚辈、上司对下属）在不同批次间不统一。
2.  **剧本概要与基调缺失 (Lack of Narrative Overview)**
    *   **现象**：LLM 每次只处理 8 行左右的字幕，不知道整部影片的类型（Genre）和核心剧情。
    *   **后果**：专业剧（如律政、医疗、科幻）的词汇选用意译化，失去了类型剧应有的专业感。
3.  **人物关系映射缺失 (Relationship Mapping)**
    *   **现象**：系统无法感知说话者 A 与听者 B 的关系。
    *   **后果**：语气词（如“您” vs “你”）和自称（如“老子” vs “我”）随 LLM 随机发挥，缺乏稳定性。
4.  **术语库单一化 (Shallow Glossary)**
    *   **现象**：术语表仅包含简单的“原文->译文”映射。
    *   **后果**：无法承载“在某种特定情境下该词应如何翻译”的复杂逻辑。

---

## 3. 改进建议：引入“全局思维” (The Global Guidance)

为了实现从“翻译工具”到“智能翻译顾问”的跨越，建议在 Pipeline 中增加 **Step 0: 全局画像扫描**。

### 3.1 核心改进方案
1.  **Global Discovery (剧情扫描器)**：
    *   在正式翻译前，增加一次“深度采样”任务，要求 LLM 生成一份包含：**影片类型、剧情梗概、主要人物画像（含性别/职能/语调）、关键人物关系图谱**的报告。
2.  **Contextual Injection (全局上下文注入)**：
    *   将上述生成的“全局画像报告”作为 `GLOBAL_GUIDANCE` 注入到后续所有直译和润色的任务中。
3.  **Role-Aware Polish (角色感知的润色)**：
    *   在润色 Prompt 中明确指令，根据全局画像调整性别代词和语气等级。

### 3.2 预期效果
*   **一致性**：全剧性别代词、人物口癖高度统一。
*   **专业度**：翻译风格能够根据剧集类型（Genre）自动切换（如：从“日常闲聊”切换到“硬核技术对话”）。
*   **地道性**：基于人物关系的语气处理，使字幕更具“影视感”。

---
**审计人**：Gemini CLI Agent
**日期**：2026-05-21
